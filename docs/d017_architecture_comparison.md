# D-017 architecture comparison matrix

See `digital-protocell/experiments/generated/d017/comparison_matrix/matrix.json` for the full evidence table.

## Hard selection outcomes

| Rule | Result |
| --- | --- |
| Select A | FAIL — no viable α; optimistic W still above ceiling-compatible source |
| Select B | FAIL — perfect-interface center W=12.69 ≥ 10; delivery insufficient |
| Reject both | **PASS** |

## Downstream revalidation (if either had been selected)

Not opened. Reject path: next directive must identify a different upstream mechanism (notably structure-turnover / constrained-radius W dominance, or broader network redesign beyond A/B).
