# D-096 Gate 6 heredity and mutation continuity

Status: `IMPLEMENTED — AWAITING FINAL ARCHITECT VERIFICATION`

This package is the bounded Gate 6 continuation authorized after the
DC-SR-004B midpoint review. It does not execute Gates 7–10 and does not alter
the certified chemistry-core equations, fission mechanics, or Phase 1
certification logic.

## Frozen contract

- Equation: `autopoietic_material_mesh_finite_catalytic_allocation_v1`
- Ordered simplex genotype: processing, activation, repair, growth
- Mutation probability: `p = 0.01`
- Mutation magnitude: `abs(Normal(0, 0.15))`
- Transfer law: `delta = min(abs(normal), source, 1 - target)`
- Mutation-off control: `mutation_none`
- Candidate identity: frozen D-096 allocation parameters

## Assays

The runnable assay is:

```text
cargo +1.89.0 run -p evolution-harness --example d096_gate6_assay
```

It checks exact inherited copies, lawful two-coordinate simplex transfer,
legal zero capped transfer, physical catalyst conservation after partition,
deterministic replicate/copy stream keys, the `(17,1)` versus `(18,0)` collision
case, and a 64-by-256 bounded uniqueness matrix. The final continuity assay
uses the actual daughters returned by `try_local_fission`: `mutation_none`
preserves genotype, candidate hash, and catalysts for processing-heavy,
repair-heavy, and neutral founders; mutation-on preserves catalyst quantities
while recording qualified-copy provenance; and three post-birth expression
steps show synthesis follows the mutated genotype. The focused Rust tests also
check that mutation metadata becomes a `MUTATION` event only when the genotype
actually changes.

## Evidence

The assay emits `D096Gate6EvidenceV1` JSON. The committed evidence record is
`d096_gate6_evidence.json` beside this report and must be regenerated by the
same command before changing the reported result.

The evidence is limited to heredity/mutation continuity. It is not a fitness,
selection, adaptation, reversal, or Phase 3 result.

The physical scope statement is precise: certified Phase 1 biology and
equations were not modified; bounded post-Phase-1 D-096 heredity and
fission-partition code in `chemistry-core` was modified and is covered by the
focused chemistry tests.

## Verification boundary

Scoped CI is defined in `.github/workflows/d096-gate6.yml`. It runs the frozen
D-096 chemistry tests, the evolution-harness suite, formatting, and the
evidence assay on the Gate 6 branch and on pull requests targeting
`strategy/d098-processing-repair`.

Gate 7 remains blocked pending final architect review of the complete package.
