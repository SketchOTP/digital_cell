# Post-M1 clean capability baseline

This branch is the proposed clean engineering base after Architect closure of M1. It is derived from the pre-M1 base and retains the accepted V4 production contract plus the reusable downstream foundations. The stacked PR #44 and its Atlas evidence remain the complete historical provenance record; this branch intentionally does not copy its dense experiments or superseded audit workflows.

Production identity is fixed at `MaturationCoupledV4` with reserve OFF. Historical V2/V3 selection remains available for certification and replay. M2 is not authorized until this baseline receives Architect acceptance.

The compact machine-readable record is `digital-protocell/experiments/generated/dcdev020postm1baseline001/baseline_manifest.json`; the explicit capability comparison and full path classification are `capability_comparison.json` and `capability_inventory.json` in the same directory. The first exact-head Linux validation passed at `9b45e41c2d5f6ed908665d53051eb73b09dad420` (run `33270482562`); a final manifest-bearing validation remains the authority for this package. Dense historical evidence remains at `/srv/ATLAS/100_ACTIVE/Projects/DIGITAL_CELL/evidence/dcdev020m1closure001/`.
