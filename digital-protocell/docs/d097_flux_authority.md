# D-097 processing flux authority

The D-096 processing contribution is active upstream:

- allocated share of H activation: `0.435257`;
- legacy share: `0.564743`;
- productive-flux elasticity to expressed processing catalyst: `0.011106`;
- processing-heavy H activation advantage: `10.518073`.

The observer-only processing knockout leaves the legacy path active and removes
the allocated contribution. The legacy-knockout decomposition leaves positive
allocated activation. Both are diagnostic calculations, not biological
evidence or production candidates.

A contract-valid full-processing shadow produced mean activation `216.063330`
but reserve change remained exactly `0`. This rules out lack of upstream
authority as the first break and independently localizes the hard block
downstream at reserve schema dispatch.
