# D-094R partial overnight audit

Status: `INVALID_INCOMPLETE_GENERATION` / provenance not established.

The takeover inventory found no live D-094 worker and no live lock at
`experiments/generated/d094r/d094_pipeline.lock`. Historical partial and
preserved campaign trees remain audit-only: their missing population
checkpoints and incomplete provenance prohibit resumption or merging. A future
run must create an immutable fresh attempt and prove source, binary,
configuration, seed, checkpoint, and lineage identity before reuse.
