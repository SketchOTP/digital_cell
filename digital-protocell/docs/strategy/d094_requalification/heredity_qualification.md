# D-094 heredity qualification

The observer uses the sealed Gate 4 mechanism-specific observables:

- `40` parent/offspring pairs;
- closed-or-present edge fraction `1.0`;
- parent/offspring edge-frequency correlation `0.7261483678362296`;
- parent/offspring network-response correlation `0.8717486739032558`;
- identifier shuffle had no effect.

The resulting `HeredityEvidenceV1` is qualified because it compares physical
edge inheritance and network response, not a non-empty string or clade label.
The observer is read-only and does not alter edge copying, partition,
fission, `mu_E`, or any chemistry equation. The sealed Gate 4 artifact is
preserved in the SR-004 import directory.
