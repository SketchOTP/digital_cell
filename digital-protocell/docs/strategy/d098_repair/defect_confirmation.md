# Defect confirmation

Frozen authority identifies the first broken link as:

```text
activated-resource production -> reserve accumulation
```

At the accepted entry head, `reserve_schema_load_ok` accepted the D-091,
D-092, D-093, and D-094 equation identities but not
`autopoietic_material_mesh_finite_catalytic_allocation_v1`. The D-096 source
already stamped valid candidates with that identity. Therefore a reserve-enabled
D-096 mesh returned `false`, `reserve_metab_step` rejected its step, and the
reserve-funded growth path returned without consuming or using reserve.

This was independently confirmed against the entry source and the sealed D-097
causal ledger. The historical D-096 artifact records zero H reserve effect and
zero reserve inflow/change for all eight pairs, while activated-resource
production was different. The focused post-repair regression retains the
fail-closed behavior for base/unknown snapshots and accepts the D-096 identity.
