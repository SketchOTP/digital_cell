# H causal attribution

Under the current mechanics/topology-on path, repair-heavy receives greater baseline-build amplification than processing-heavy in every paired seed. `baseline_repair_gain_leakage_present=true`.

The per-seed paired values, reserve-funded growth contrasts, structural-build contrasts, final structural mass, final total material, and baseline-amplification fractions are recorded in `h_causal_attribution.json`.

The fixed shadow removes the repair-heavy H structural-mass advantage in all eight paired seeds. This is a causal attribution result, not a reproductive or fitness qualification.
