# Legacy preservation

The repair condition is explicit and finite-allocation-only. The base material mesh, D-089 composition, D-092 template, D-093 network, and D-094 autocatalytic-set branches retain their existing `g_strain * gb` behavior. The free-membrane production branch still uses its prior coordinate-2 gain and was not broadened by R4.

Focused regression `d096_historical_mesh_build_path_is_unchanged` passed. The R4 workflow also runs the existing chemistry and harness regression suites.
