# D-096 Gate 7 — single-generation reproductive consequence

Gate 7 executes the accepted D-096 Gate 6 candidate identities under the
frozen H, B, and Neutral forcing. It observes only whether the founder's
existing material dynamics produce a first physical fission, founder death,
or the preregistered horizon.

The campaign began from `f4adec147f6d8c0763d1264b8954eaf2788edcca`, used
`mutation_none`, paired seeds `1..=16`, and stopped each replicate at the
first founder fission/death or 4,000 accepted steps (`80.0` nominal simulated
time). Daughters were never advanced.

The frozen campaign observed no fission and no founder death in any of the
144 cells. The governed conclusion is therefore
`SR004C_D096_GATE7_UNTESTABLE_IN_FROZEN_HORIZON`. No fitness scalar, winner,
selection operator, or Gate 8 logic was introduced or executed.

Evidence is under `digital-protocell/experiments/generated/sr004c/`.

