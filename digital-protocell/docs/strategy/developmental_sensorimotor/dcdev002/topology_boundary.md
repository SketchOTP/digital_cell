# Topology boundary

The network is initialized for a fixed vertex count.  A frame with another
count returns `TopologyChangeUnsupported` before state or ledger mutation.
There is no remapping, duplication, averaging, or inheritance across remesh,
vertex split/merge, or fission.  Those are future scientific questions.
