# Limitations

The mapping is intentionally bounded and conservative. It supports the existing closed-ring remesh representation and does not claim arbitrary topology correspondence. It does not infer organism identity, decide remesh events, or transfer state across fission. Full workspace testing retains the pre-existing missing D-008 fixture limitation.
