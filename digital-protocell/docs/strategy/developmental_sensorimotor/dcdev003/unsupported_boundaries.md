# Unsupported boundaries

Fission remains out of scope. `TopologyEventV1::Fission` returns `UnsupportedTopologyEvent` before state transfer, so no daughter-state inheritance is invented. `TopologyEventV1::Unknown` fails the same way.

This directive adds no external sensing, effector, motor, learning, memory, identity, evolution, or regulatory inheritance capability.
