# Selected architecture

## Conclusion

`DCDEV001_HYBRID_COMPOSITION_SELECTED`

The selected architecture is a Digital-Cell-native distributed regulatory patch system informed by external patterns. It composes Digital Cell's material authority with `REFERENCE` patterns from ALIEN, NCA, and DISHTINY, while keeping the material mesh, chemistry, transport, growth, and fission as the only causal organism authority.

## First-slice selection

* `BUILD` persistent local regulatory state coupled to bounded material patches;
* `BUILD` one local non-semantic transducer for already-authoritative physical quantities;
* `COMPOSE` bounded neighbor-to-neighbor propagation;
* `BUILD` deterministic accepted-time updates and append-only provenance.

## Deferred or rejected

Effector and motor behavior are `DEFER`RED from the first slice. External source code, external runtime dependencies, genome-built bodies, CA replacement meshes, central controllers, target shapes, fitness scalars, selection campaigns, learning, memory claims, and D-096 re-entry are `REJECT`ED or outside this directive. A later local graph is `DEFER`RED.
