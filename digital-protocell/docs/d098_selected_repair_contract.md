# D-098 finite-allocation processing implementation repair contract

Status: `FROZEN_NOT_IMPLEMENTED`.

## Authorized defect repair

Add
`autopoietic_material_mesh_finite_catalytic_allocation_v1` to the explicit
reserve-compatible equation dispatch used by `reserve_schema_load_ok`.

No other runtime biology is authorized.

## Required regression behavior

- A valid D-096 mesh with reserve enabled passes reserve schema compatibility.
- Unstamped historical meshes still fail closed.
- D-091 through D-094 compatibility behavior remains unchanged.
- D-096 reserve `A -> R`, reserve outflow, and reserve-funded growth execute and
  close accounting.
- D-096 snapshot isolation and candidate hashes remain unchanged.
- The frozen H pulse, B damage, allocation vectors, total budget, expression
  costs, mutation contract, and mesh body remain byte-for-byte unchanged.

## Required replay

After the bounded source repair, rerun D-096 Gate 5 only with the original
paired seeds, stopping rule, mutation-off and fission-off controls. Report the
same continuous H, B, and neutral effects. Do not proceed to D-096 Gate 6 unless
the repaired reciprocal Gate 5 passes unchanged criteria.

## Exclusions

No environment strengthening, catalytic tuning, transport redesign, regulation,
heredity implementation, population campaign, adaptation, reversal, or Phase 3
authorization.
