# D-096 finite allocation implementation

The frozen `autopoietic_material_mesh_finite_catalytic_allocation_v1` equation was
implemented without changing the frozen contract. The inherited four-coordinate
simplex is stored in `MaterialMesh`, stamped with schema 2, and isolated from all
historical snapshots. Candidate identity hashes the ordered IEEE-754 allocation
and every frozen parameter.

Expression uses the frozen synthesis, activation, maintenance, and turnover
rates. Rejected expression steps are transactional. Fission copies the inherited
allocation and expressed catalytic state through the existing conservative
daughter construction. No treatment label, fitness variable, regulation,
transport allocation, or Phase 1 body change was added.

Gates 0–4 passed. Gate 5 failed and stopped the directive.
